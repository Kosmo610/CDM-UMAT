# 해석 대기열 — 워크스테이션에 앉으면 이 순서대로

**a3(총괄)가 유지한다. 2026-08-13 기준.** 솔버가 필요한 것만 모았다.
솔버 없이 되는 일은 `paper/review/TO_A1.md` R4 로 이미 발행되어 두 에이전트가
돌리고 있으므로, **여기 있는 것만 사람이 하면 된다.**

각 항목은 **왜 지금인지 / 무엇이 열리는지 / 안 하면 무엇이 막히는지**를 달았다.
번호는 우선순위다.

---

## Q1. ★ M7 — T500 재도전 (M6 의 유일한 미완주)

| | |
|---|---|
| 묶음 | **아직 없다.** a2 의 R4-A6 이 덱을 준비 중 |
| 여는 것 | S1 완결 → **S3·S4·S5 전부** |
| 안 하면 | 세 온도 중 하나가 비어 보정이 미완으로 남는다 |

M6 에서 RT23·T1000 은 완주, **T500 만 30.4 분에 죽었다.** 잔차의 20 %가
자유 드라이버 eps_yy 의 **0.0408 MPa** — M3 때와 같은 "솔버가 물리적으로
무의미한 정밀도를 요구하는" 서명이다. 물성 문제가 아니다.

**a2 가 R4-A6 으로 덱 수정을 준비하고 있으니, 그것이 올라온 뒤에 돌린다.**
지금 M6 T500 을 그대로 다시 돌리면 같은 자리에서 또 죽는다.

---

## Q2. ITAN·취성 봉 9잡 — `dist` 아닌 채팅 전달분

| | |
|---|---|
| 묶음 | `LTH_ITAN_0812_1411.zip` (채팅으로 전달, 미실행) |
| 여는 것 | ①본해석에서 강성행렬을 켤지 결정 ②균열대 정규화 판정 |
| 안 하면 | 제3장 §3.8-5 결론이 미완, 본해석 수렴 비용이 미지수 |
| 비용 | 9잡 전부 몇 초~몇 분. 총 30분 이내 |

**판정 순서를 반드시 지킬 것** (RUN_ME.md 에 같은 내용이 있다):
1. `compare_tangent.py` — ITAN 0/1 쌍 세 개. **답이 같은지부터** 판정한다.
2. `cband_damage.py` — `_BR` 봉 3개. **밴드가 한 줄인지 먼저.**
3. `patch_report.py` — 위가 통과한 뒤에야 에너지를 읽는다.
4. **정규화 판정은 `_BR` 봉으로만.** `_ITAN` 봉은 소성이 섞여 있다
   (N=5 에서 소산의 90.5 %).

올릴 파일: `LTH_CBAND_N{5,10,20}_tangent.csv`, `cband_curves.csv`.

---

## Q3. `kbar_summary.csv` 회수 — 해석 불요, 파일 찾기만

| | |
|---|---|
| 상태 | **잡은 2026-08-07 에 이미 돌았다.** 값(5.449)도 확정 |
| 여는 것 | [그림 4.5] + admissible 판정 기록 |
| 안 하면 | prerun_gate 의 유일한 차단 항목이 계속 열려 있다 |
| 비용 | 5분 (없으면 `LTH_RUN2` 재실행, 짧다) |

`E:\LTH\LTH_RUN2_0807_1830\` 에서 `*_summary.csv` 를 찾아 커밋하면 끝난다.

---

## Q4. CAE 캡처·죽은 ODB 회수 — 해석 불요

| 그림 | 필요한 것 | 비고 |
|---|---|---|
| 4.1 RVE 메시 | CAE 뷰포트 캡처 3장 (기하 / 사면체 절단면 / `.ori` 방향) | 200 dpi 이상, `docs/figures/` |
| 4.4 냉각 TRS 필드 | **죽은 M5 잡의 `.odb` 로 충분** — 냉각 스텝은 온전하다 | 기지 S11 컨투어 |

a1 이 캡션과 본문 삽입을 기다리고 있다. **새 해석이 필요 없다.**

---

## Q5. 사이클 점프 검증 — `LTH_CJ2_0812_1411`

| | |
|---|---|
| 묶음 | `dist/LTH_CJ2_0812_1411.zip` (덱 5개: HEAT_SH/SM/SL, JMP, EXP) |
| 여는 것 | cycle jump 구조 오차를 **가정이 아니라 측정**으로 (제7장 한계 6번) |
| 안 하면 | 한계 6번이 "측정하지 않음"으로 남는다 |

S0 선행검증의 마지막 항목이다. **9케이스 매트릭스 전에** 끝나 있어야 한다.

---

## Q6. (M7 이후) S3 거시 역학 9잡 + S4 대조 3잡

**아직 시키면 안 된다.** 거시 역학 카드가 여전히 자리표(placeholder)다.
카드가 자리표면 손상이 안 걸려 세 TRS 케이스가 똑같이 나오고, 그러면
"차이가 작다"가 결과가 아니라 인공물이 된다 (prerun_gate S3 관문).

**순서: Q1(M7) → 유효물성 확정 → 거시 카드 재생성 → 그때 S3.**

---

## 지금 이 순간의 상태 요약

```
솔버 불요 (에이전트가 진행 중)   ████████░░  R4 10건 중 3건 착수 확인
솔버 필요 (사람 대기)            ░░░░░░░░░░  Q1~Q5, 그중 Q3·Q4 는 회수만
```

- **Q3·Q4 는 해석이 아니라 파일 회수다.** 앉자마자 5분이면 둘 다 끝난다.
- **Q2 는 30분이면 끝나고 두 가지 판정을 동시에 연다.**
- **Q1(M7)은 a2 의 덱 수정을 기다린다** — 먼저 돌리면 같은 자리에서 죽는다.
