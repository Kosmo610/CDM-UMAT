# 해석 대기열 — 워크스테이션에 앉으면 이 순서대로

**a3(총괄)가 유지한다. 2026-08-13 기준.** 솔버가 필요한 것만 모았다.
솔버 없이 되는 일은 `paper/review/TO_A1.md` R4 로 이미 발행되어 두 에이전트가
돌리고 있으므로, **여기 있는 것만 사람이 하면 된다.**

각 항목은 **왜 지금인지 / 무엇이 열리는지 / 안 하면 무엇이 막히는지**를 달았다.
번호는 우선순위다.

---

## Q1. ★ M7 — T500 재도전 (M6 의 유일한 미완주)

| | |
|---|---|
| 묶음 | **아직 없다.** 프리셋(`--m7`)은 정해졌으나 zip 이 안 나왔다 — 아래 8/16 갱신 |
| 여는 것 | S1 완결 → **S3·S4·S5 전부** |
| 안 하면 | 세 온도 중 하나가 비어 보정이 미완으로 남는다 |

### ★ 2026-08-14 정정 — **진단이 뒤집혔다. 허용오차 문제가 아니다.**

전에는 「M3 와 같은 서명 = 솔버가 무의미한 정밀도를 요구한다」로 적었다.
a2 의 R4-A6(`9b03a0d`)이 **그것을 실측으로 부정했다**:

- M3 의 `*CONTROLS` 완화는 **이미 M6 덱에 들어가 있었다** — 세 스텝 모두
  $R_n$ = 0.02 (`abaqus/M6_CONTROLS.txt`, 배포 덱에서 sha256 과 함께 그대로 인용).
- 그런데도 죽었고, **그 잔차를 통과시키려면 $R_n$ = 1.47 이 필요하다.**
  그것은 허용오차가 아니라 **평형을 포기하는 값**이다.
- 즉 **같은 증상, 다른 병**이다. **허용오차 레버는 이미 다 썼다.**

**M7 의 레버는 `stabilize` 이며, `allsdtol = 0.05` 로 묶는다**(인공 감쇠가 피크를
부풀리는 것을 막는 CLAUDE.md 규칙). `msg_residual_census` 가 이제 이 판정을
스스로 내려 CSV 에 `TOLERANCE` / `EQUILIBRIUM` 를 써 준다.

### 2026-08-16 갱신 — **프리셋은 정해졌다. 묶음이 아직 없다.**

a2 `f9ee9a5` 가 `retune_deck.py --m7` 을 넣었다. 두 가지만 바꾼다:

| | M6 | **M7** | 성격 |
|---|---|---|---|
| 얀 $G_{tc}$ | 0.107 | **0** | **적법성 수정** — 여기서 무해함이 증명돼 있다 |
| `stabilize` | 2e-4 | **1e-3** | **거동을 바꾸는 유일한 항목.** 5배 한 걸음만 |

한 번에 하나만 움직이는 것이 요점이다 — 둘을 같이 바꾸면 결과를 어디에
돌릴지 알 수 없다. `ftol` 은 그대로다.

**그러나 `dist/` 에 M7 zip 이 없으므로 아직 못 돌린다.** 스위치는 덱을
만드는 도구이지 워크스테이션에 풀 묶음이 아니다. **a2 에게 묶음을 요청했다**
(`TO_A1.md` 머리말 B-2). 오면 `LTH_M7_<MMDD_HHMM>` 이름으로 온다.

**받으면 반드시 확인할 것 — `ALLSD/ALLIE` 가 출력에 있는지.** `stabilize` 를
켰으므로 인공 감쇠가 5 % 미만인지 못 보면 **피크를 논문에 못 싣는다.**

→ **따라서 Q1 은 아직 못 돌린다.** 막는 것이 「덱 수정」에서
   **「`stabilize` 를 넣은 M7 덱을 만들어 zip 으로 내보내기」** 로 바뀌었을 뿐이다.
   **이것이 지금 사용자를 막고 있는 유일한 항목이다.**

---

## Q2. ITAN·취성 봉 9잡 — `dist` 아닌 채팅 전달분

| | |
|---|---|
| 묶음 | `LTH_ITAN_0812_1411.zip` (채팅으로 전달, 미실행) |
| 여는 것 | ①본해석에서 강성행렬을 켤지 결정 ②균열대 정규화 판정 |
| 안 하면 | 제3장 §3.8-5 결론이 미완, 본해석 수렴 비용이 미지수 |
| 비용 | 9잡 전부 몇 초~몇 분. 총 30분 이내 |

**판정 순서를 반드시 지킬 것** (RUN_ME.md 에 같은 내용이 있다):
1. `compare_tangent.py` — ITAN 0/1 쌍 세 개. **답이 같은지부터** 판정한다.
2. `cband_damage.py` — `_BR` 봉 3개. **밴드가 한 줄인지 먼저.**
3. `patch_report.py` — 위가 통과한 뒤에야 에너지를 읽는다.
4. **정규화 판정은 `_BR` 봉으로만.** `_ITAN` 봉은 소성이 섞여 있다
   (N=5 에서 소산의 90.5 %).

올릴 파일: `LTH_CBAND_N{5,10,20}_tangent.csv`, `cband_curves.csv`.

---

## Q3. `kbar_summary.csv` 회수 — 해석 불요, 파일 찾기만

| | |
|---|---|
| 상태 | **잡은 2026-08-07 에 이미 돌았다.** 값(5.449)도 확정 |
| 여는 것 | [그림 4.5] + admissible 판정 기록 |
| 안 하면 | prerun_gate 의 유일한 차단 항목이 계속 열려 있다 |
| 비용 | 5분 (없으면 `LTH_RUN2` 재실행, 짧다) |

`E:\LTH\LTH_RUN2_0807_1830\` 에서 `*_summary.csv` 를 찾아 커밋하면 끝난다.

### ~~Q3-b. M6 응력-변형률 곡선 3개~~ — ✅ **2026-08-14 해소, 사용자 할 일 없음**

a2 가 `9b03a0d` 로 세 곡선을 **직접 커밋했다**(RT23 540행 · T500 357행 ·
T1000 409행). `.gitignore` 의 `*_ss.csv` 도 이 셋만 예외로 뚫었다.
`check_ch4_numbers` 적색 3건이 사라졌고 **항목이 105 → 160 으로 늘었다.**
아래 원문은 기록으로 남긴다.

<details><summary>원래 항목 (클릭)</summary>

#### Q3-b. (2026-08-13 추가) M6 응력-변형률 곡선 3개도 같이 회수한다

| | |
|---|---|
| 상태 | **잡은 2026-08-12 에 돌았다.** 제4장이 이미 그 값을 싣고 있다 |
| 필요한 파일 | `LTH_M6_RT23_ss.csv` · `LTH_M6_T500_ss.csv` · `LTH_M6_T1000_ss.csv` |
| 여는 것 | `check_ch4_numbers.py` 적색 3건 해소 (지금 커밋이 막힌다) |
| 안 하면 | 제4장의 174.2 / 172.7 / 235.2 GPa 가 **1차 기록 없이** 실린 상태로 남는다 |

M6 묶음 폴더에서 `*_ss.csv` 를 찾아 올리기만 하면 된다. **Q3 과 같은 자리에서
한 번에 처리한다.** 자세한 사정은 `TO_A1.md` R6-1.

</details>

---

## Q3-c. ★ (2026-08-15 신규) 손상 상한 후처리 — **해석 불요, 잡당 몇 초**

| | |
|---|---|
| 묶음 | **`dist/LTH_DMGFIX_0815_2345.zip`** (a2 `c37368d`) |
| 무엇 | 고친 `damage_map.py` + 새 `damage_ceiling.py` — **이미 있는 `.odb` 를 다시 훑기만 한다** |
| 여는 것 | A-4 의 셋째 물음(**피크가 얼마나 부풀려졌나**)의 상한 추정 |
| 안 하면 | 제4장 강도 절대값이 「잠정」으로 계속 남는다 |
| 비용 | **잡당 몇 초.** 솔버는 돌지 않는다 |

**왜 새 해석이 필요 없나** — 막혀 있던 것은 계산이 아니라 **버그**였다.
`damage_map` 의 부피 구간 상단이 0.90 에 `>=` 로 걸려 있었는데, 무르는 법칙은
0.90 에 **닿기만 하고 넘지 않는다.** 그래서 그 구간은 **구조적으로 절대 안 잡히고**
모든 잡이 모든 상에서 0 을 보고했다. 고친 판은 `volfrac_at_cap` 과
`peak_inflation_bound` 를 따로 적는다.

**Q4 와 같은 자리에서 함께 처리한다.** 둘 다 기존 `.odb` 만 읽는다.

---

## Q4. CAE 캡처·죽은 ODB 회수 — 해석 불요

| 그림 | 필요한 것 | 비고 |
|---|---|---|
| 4.1 RVE 메시 | CAE 뷰포트 캡처 3장 (기하 / 사면체 절단면 / `.ori` 방향) | 200 dpi 이상, `docs/figures/` |
| 4.4 냉각 TRS 필드 | **죽은 M5 잡의 `.odb` 로 충분** — 냉각 스텝은 온전하다 | 기지 S11 컨투어 |

a1 이 캡션과 본문 삽입을 기다리고 있다. **새 해석이 필요 없다.**

---

## Q5. 사이클 점프 검증 — `LTH_CJ2_0812_1411`

| | |
|---|---|
| 묶음 | `dist/LTH_CJ2_0812_1411.zip` (덱 5개: HEAT_SH/SM/SL, JMP, EXP) |
| 여는 것 | cycle jump 구조 오차를 **가정이 아니라 측정**으로 (제7장 한계 6번) |
| 안 하면 | 한계 6번이 "측정하지 않음"으로 남는다 |

S0 선행검증의 마지막 항목이다. **9케이스 매트릭스 전에** 끝나 있어야 한다.

---

## Q6. (M7 이후) S3 거시 역학 9잡 + S4 대조 3잡

**아직 시키면 안 된다.** 거시 역학 카드가 여전히 자리표(placeholder)다.
카드가 자리표면 손상이 안 걸려 세 TRS 케이스가 똑같이 나오고, 그러면
"차이가 작다"가 결과가 아니라 인공물이 된다 (prerun_gate S3 관문).

**순서: Q1(M7) → 유효물성 확정 → 거시 카드 재생성 → 그때 S3.**

---

## 지금 이 순간의 상태 요약

```
솔버 불요 (에이전트가 진행 중)   █████████░  R4 10건 중 9건 완료 (남은 것 A-1)
솔버 필요 (사람 대기)            ░░░░░░░░░░  Q1~Q5, 그중 Q3·Q3-c·Q4 는 회수·후처리만
```

**2026-08-16 기준 — 앉으면 바로 되는 순서**

| | 할 일 | 비용 | 지금 되나 |
|---|---|---|---|
| Q3 | `kbar_summary.csv` 회수 | 5분 | ✅ |
| ~~Q3-b~~ | ~~M6 곡선 3개~~ | — | ✅ **a2 가 커밋함, 할 일 없음** |
| **Q3-c** | **`LTH_DMGFIX_0815_2345.zip` 후처리** | **잡당 몇 초** | ✅ **해석 아님** |
| Q4 | CAE 캡처 3장 + 죽은 M5 ODB | 10분 | ✅ Q3-c 와 같은 자리 |
| Q2 | ITAN·취성 봉 9잡 | 30분 | ✅ |
| Q5 | 사이클 점프 5덱 | 1시간 | ✅ |
| Q1 | M7 | 큼 | ⛔ **프리셋은 왔다. a2 가 zip 을 내보내야 한다** |
| Q6 | S3 9잡 | 큼 | ⛔ 카드 자리표 — 금지 |

**Q3-c 의 값이 8/16 에 올라갔다.** 처음에는 「강도 절대값의 잠정 딱지」만
걸려 있었는데, a2 가 제3장 §3.8-8 에서 **제6장 C1 결론까지 여기 걸려 있음**을
보였다 — 상한에 닿은 요소는 재분배를 못 하므로 **B 와 C 가 인위적으로 붙는다.**
「차이가 작다 → 단순화가 정당하다」는 결론은 **`volfrac_at_cap` 을 재기 전에는
쓸 수 없다.** 그 값을 내는 것이 바로 Q3-c 다.
