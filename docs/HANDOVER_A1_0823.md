# a1 세션 인수인계 — 2026-08-23 14:08 KST

> **읽는 사람에게:** 너는 이 프로젝트의 **에이전트 1(a1, 논문 조사·물성 정리)**이다.
> 이 문서는 구 a1 세션(`session_01VwwEP1aVmkbfWA34KDhjUj`, 2026-08-03 개시)이
> **네트워크 정책이 열린 새 세션**으로 업무 전부를 넘기는 인수인계서다.
> 이 문서가 커밋된 시점 이후 **구 세션은 관찰 전용이다** — 커밋·푸시·우편함
> 발신을 하지 않는다. 브랜치가 곧 정체성이므로, 네가 이 브랜치에서 작업하는
> 순간 a2·a3 와의 연결은 자동으로 이어진다 (저장소가 유일한 통신선).

## 1. 정체성 — 30초 요약

1. 브랜치: **`claude/paper-reference-research-pksw5p`** 에서만 개발·커밋·푸시.
2. 규칙 정본은 **`CLAUDE.md`** — 응답 형식(★ §1~9, 모든 채팅에 적용), 세 에이전트
   구조와 판정권 분배표, 해석 파일 전달 규칙, 검증 명령 목록(커밋 전 전수 통과).
3. 세션 시작 시와 커밋 전: `python3 sync/sync_check.py`. **a2 소유 4파일**
   (`sync/sync_check.py`·`PROTOCOL.md`·`outbox_a2.json`·`state_a2.json`) 편집 금지.
4. a3 지시서는 `paper/review/TO_A1.md` (정본은 a3 브랜치
   `claude/llm-task-decomposition-w41jpc`, 내 브랜치에 사본 유지 + 자기 항목에
   `✅ <commit>` 표시 — R7-4 관례).

## 2. 인수 시점 상태 (구 세션 마지막 실측)

| | |
|---|---|
| 구 세션 HEAD | `6947720` (이 문서 커밋 직전) |
| 장부(자동 검증 항목) | **3494** — 병합 트리 실측, 미러 5곳(제1·3·7장, NOVELTY, check_chapter_consistency) 동기화됨 |
| 게이트 | **78개 전수 통과, 적색 0** (구 세션 소화분 기준) |
| PR | **#4** (Kosmo610/CDM-UMAT, base `claude/session-boyy66`, **초안**) — 큰 병합·판정마다 본문 갱신해 온 관례 |
| a3 소화 | **R18까지** (a3 최신은 R24 — 아래 백로그) |
| a2 병합 | `4b3b21d`까지 (이후 8커밋 미병합 — 아래 백로그) |
| 우편함 | a2-0046까지 ack, **a2-0047~0052 6통 미처리** |

## 3. ★ 미처리 백로그 — 우선순위순 (첫 일감)

1. **a2 병합** — `HEAD..a2` 8커밋(`c8e468c`…`b3b6b6c`, 시험 분위기 census 계열:
   X(T) 기울기 재소싱, [5]진공/[10]산화 중재, §6.6 분위기 구조 분리,
   `atmosphere_census.py` 신설). 병합 → `check_ch3_numbers` 재계산 → 미러 5곳
   동기화 → 게이트 전수 → 커밋 메시지에 "병합 트리 실측" 명시.
2. **우편함 6통 처리** — a2-0047~0052. 특히 a2-0052 는 **"분류 판정은 그쪽(a1)
   몫"** 이라고 명시했다(하중지지 출처의 시험 분위기 분류). 본문은
   `sync/outbox_a2.json` 에 전부 있다. 반영 후 `--ack`.
3. **a3 R19~R24 소화** — 정본은 a3 브랜치의 `paper/review/TO_A1.md` (`817ea2e`).
   - **R23 ⛔ 최우선**: §6.6.2-a 에 「상한」이 되살아났다 — 구 세션이 뒤집었던
     부호 사고의 재발이다. 처리하면서 **check_ch6_numbers F절이 왜 못 잡았는지**
     도 확인하고 검사를 보강할 것.
   - **R24 [a1], 사용자 승인**: 표 번호 체계 실행 지시서.
   - R19~R22: 이행 확인·규칙의 표 이관·사용자 실행 개시에 따른 대기열 교정·논문
     뼈대 발행. [a1]/[공통] 항목만 순서대로, 한 건당 한 커밋, 커밋 메시지에 R번호.
   - 소화 후 TO_A1.md 사본을 a3 최신으로 동기화하고 자기 항목에 ✅.
4. **OA 논문 2건 직접 다운로드** — 새 환경에서 비로소 가능해진 일:
   - *Exp. Mech.* (2023), DOI `10.1007/s11340-023-00962-x` — 후보 23, SiC/SiC
     편조관 열충격 사이클+잔여강도. **경향 대조 전용** (재료·구조 다름).
   - *Ceramics-Silikáty* **63**(3) (2019), DOI `10.13168/cs.2019.0028` — 후보 24,
     Li, 온도의존 PLS. `pls_validation.py` 논리의 방법론 원전 계열.
   - 번호는 **refs/README.md 색인이 정본** — 다음 빈 번호(74부터로 보이나 부여
     전 색인 확인). 파일명 관례 `[NN] 설명.pdf`, 등급 `fulltext`, 색인 갱신 후
     `python3 data/literature/refs_audit.py --check` 통과. 상세 배경은
     `docs/DOWNLOAD_LIST_RECENT.md` §23·§24.
5. **사용자 대기물 추적** — R21 이후 사용자가 실제로 돌리기 시작했다.
   Q1=M8 3잡(`dist/LTH_M8_0818_1231.zip`) · Q3(kbar_summary.csv 회수) ·
   Q3-c(damage_ceiling 재판독) · Q4(CAE 캡처). 결과 CSV가 오는 대로 판정.

## 4. 결과가 와야 움직이는 판정 대기

1. **M8 결과** → 접선·강도·잔차 판정, §6.6.2 「가정→측정」 전환(제7장 한계 14도
   같이), `m6_verdict` 4b의 SUPPLIES/REFUSED 확인, 잔여강도 절대값 인용 가부.
2. **Q3-c CSV** → 제4장 강도 절대값 「잠정」 딱지 제거 판정.
3. **§7.5 종합 진술** → §7.2.2 채움 이후 (a1+a2 공동, `pending_slots.py` 참조).

## 5. 체크인 크론 재생성 (새 세션에서 self-bind 로)

하루 4번 고정(사용자 지정): KST 06:30 · 13:00 · 19:00 · 23:00
= UTC cron **`30 21 * * *`** 와 **`0 4,10,14 * * *`** 두 개.
서버가 발동을 +5~7분 흩뜨린다(조절 불가). 프롬프트는 아래 원문을 쓰되
기준선 줄만 인수 시점으로 갱신할 것:

```
[정기 체크인 · a1 문헌·물성] 우편함과 지시서를 확인하고, 변한 것이 없으면
사용자에게 아무 말도 하지 말고 조용히 끝낸다(CLAUDE.md 규칙). 이 체크인
안에서 send_later 로 다음 회차를 다시 잡지 않는다 — 크론이 한다.

확인 절차 (FETCH_HEAD 를 쓰지 말 것 — 연속 fetch 하면 뒤 것이 앞 것을 덮어써
엉뚱한 diff 가 나온다. 반드시 명명된 remote ref 로):
  python3 sync/sync_check.py
  git fetch -q origin +claude/llm-task-decomposition-w41jpc:refs/remotes/a3/head \
    && git log -1 --format=%h a3/head \
    && git show a3/head:paper/review/TO_A1.md | grep -n "^## R[0-9]"
  git fetch -q origin +claude/thesis-csic-thermal-shock-5m53iv:refs/remotes/a2/head \
    && git log --oneline HEAD..a2/head

할 일: ① a3 지시서에 새 회차가 있으면 [a1]/[공통] 항목만 읽고 순서대로,
한 건당 한 커밋으로 처리하며 커밋 메시지에 R번호를 적는다. ② a2 새 커밋이
있으면 병합 → check_ch3_numbers 로 장부 재계산 → 미러 5곳(제1·3·7장,
NOVELTY, check_chapter_consistency) 동기화 → 게이트 전수 → 커밋 메시지에
"병합 트리 실측" 명시. ③ PR #4(Kosmo610/CDM-UMAT)가 머지/클로즈됐는지
확인. 이 저장소에는 GitHub Actions 가 없어 체크런은 항상 0이다.

기준선: 인수 시점 = docs/HANDOVER_A1_0823.md §2·§3 (a3 817ea2e R24,
a2 b3b6b6c, 우편함 a2-0052까지 수신).
```

## 6. 구 세션에서 배운 함정 — CLAUDE.md 에 아직 없는 것만

1. **방향은 기준선 없이 존재하지 않는다** (a3 R18-1). 오차 방향을 적을 때
   무엇에 견준 방향인지 같은 문장에 적는다. 부호 사고(§6.6.2)가 이걸로 났고,
   R23 재발도 같은 계열이다.
2. **자리표는 값의 부재가 아니라 다른 값의 선언이다** (R17-2).
   `verification/placeholder_directions.csv` 가 그 장부다.
3. **질문이 낡았는지 실행 덱을 먼저 연다.** "Gtt를 켤까"를 판정하러 갔더니
   M6부터 이미 켜져 있었다 (2026-08-19, `yarn_fracture_energy.py` §6).
4. **인용 금지 목록**: 출처 없는 $G_{tc}/G_{tt} \approx 14.2$ · $-130.84 \pm
   34.53$ MPa · Research Square 프리프린트 `rs-9997234/v1`.
5. refs 에 없는 문헌은 번호 없이 쓰되 "(refs 미보유)" 를 한 번 적는다.
6. 문서 전달은 PDF(`postprocess/md_to_pdf.py`), 파일명 `이름_MMDD_HHMM`(KST),
   변환 후 `pdftoppm` 으로 1페이지 눈 확인.

## 7. 구 세션 처리

이 커밋 이후 구 세션은 커밋·푸시·발신을 하지 않는다. 구 세션의 체크인이 남아
돌면 조용히 종료만 반복한다. 새 세션 크론이 정상 동작하는 것을 확인한 뒤,
사용자가 구 세션에 마지막 지시로 정리를 맡기면 된다.
