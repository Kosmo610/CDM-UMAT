# P4 배치 — 얀 종방향 연화지수 A1T 를 크랙밴드 상당값으로 (2.0 → 50.0)

**변수는 하나.** 얀 카드 `PROPS(18)`. 나머지 전부 P3 과 동일 —
같은 XT(2835), 같은 Depvar(17), 같은 메쉬, 같은 span, 같은 eta,
같은 UMAT(V2_7D).

**사전등록 완료.** 예측은 `verification/PAPER_DEVIATION_REGISTER.md`
§5.25 에 배치 전에 적혀 있다.

## 왜 이 배치인가

P3 가 남긴 실패항목 셋이 **같은 뿌리**를 가리킨다.

1. 곡선 형상 — 23/500 °C 가 국소최대 후 **재경화**해 0.85 % 끝까지
   상승 마감 (논문은 ~0.3 % 에서 종국 파단)
2. 절대 강도 초과 — +53/+20/+22 %
3. warpL 온도 추세 — 논문 하강(60.67→32.12), 우리 평탄~상승

뿌리: **얀 종방향이 개시(전단 주도, SDV17 실측 0.92~0.97)는 하는데
그 뒤 연화가 너무 완만해(A1T=2.0) 관측창 안에서 안 끊어진다.**

수치로: `d = 1 − exp[A(1−r)]/r` 에서 r=1.05 일 때
A=2 → d=0.14, **A=50 → d=0.92.** A=50 이면 개시 5 % 뒤 사실상 절단.

## 왜 하필 50 인가 — 자의적 값이 아니다

V2_6 크랙밴드가 논문 XT 에서 주는 값이 정확히 50 이다:

```
g0 = XT^2/(2*E1) = 2835^2/(2*254967) = 15.76 N/mm^2
g0*le (le~0.059mm) = 0.93  >>  GF1T = 0.03962
-> A1TEFF = 2*g0*le/(GF1T - g0*le) < 0  ->  클램프 50   (전 요소)
```

이 메쉬의 모든 요소에서 클램프에 걸리므로 **"크랙밴드 켜기" 와
"A1T=50" 은 수치적으로 동일하다.** 슬롯 32 를 늘리는 덱 수술 대신
기존 슬롯 18 하나를 바꾼다. UMAT 수정도 없다.

## 한 배치로 답하는 질문 (§5 요건 — 7개)

1. 재경화가 사라지는가 (곡선 형상)
2. 강도 절대값이 논문 쪽으로 내려오는가
3. 최대점 변형률이 논문(0.32/0.26/0.31 %)에 접근하는가
4. warpL 손상요소율과 그 온도 추세가 바뀌는가 (국부화 예상)
5. 냉각이 P3 와 비트 일치를 유지하는가 (A1T 는 개시 r 에 안 들어감)
6. SDV17 전단분율이 유지되는가 (개시 기구는 A1T 무관이어야 함)
7. 손상 후 HOM E11 이 P3(27.6 GPa)보다 낮아지는가

## 준비물 — E:\LTH 에 있어야 할 것

1. `setup_p4.py` — zip. ★ 이것만 실행하면 된다
2. `patch_depvar_yarn.py` / `patch_material_prop.py` — zip (setup 이 부름)
3. `Try_P3\` `Try_P3T500\` `Try_P3T1000\` 의 덱 + `.ori` + UMAT —
   **P3 배치가 만든 것.** setup 이 P4 폴더로 복사한다

## 실행 (순차적 진행 — 두 줄)

```
cd /d E:\LTH
python setup_p4.py
```

미리보기를 보고 이상 없으면:

```
python setup_p4.py --apply
```

발사 명령까지 찍어준다. 자체시험(44검사)은 자동으로 돈다.

setup 이 확인하는 것:

1. **P3 계보** — XT=2835 + Depvar 17. 아니면 중단
2. **GF1T(슬롯 32)가 없는가** — ★ 이 배치의 사활. 있으면 중단
3. 슬롯 18 만 2.0→50.0 (이웃 19/20/21 의 2.0 은 그대로)
4. 얀 SDV 이름배치 정정 (해석 무영향, 추출 사고 예방)
5. 멱등, 부분적용 중단

### 2번이 왜 사활인가

UMAT V2_7D 370-380행:

```fortran
IF (GF1T.GT.0.0D0) THEN
   ...  A1TEFF = 크랙밴드 계산
ELSE
   A1TEFF = A1T          <- PROPS(18). 여기로 와야 P4 가 성립
END IF
```

**GF1T 가 살아 있으면 `PROPS(18)` 은 통째로 무시된다.** 그 상태로
A1T 를 바꾸면 이틀을 태우고 P3 와 똑같은 결과가 나온다. P3 덱은
상수 31개(슬롯 32 없음)이므로 정상이지만, 확인 없이 믿지 않는다.

### 4번 — 이름배치 정정을 하는 이유

`*Depvar` 이름줄은 출력 라벨일 뿐 UMAT 에 안 들어간다 (STATEV 는
번호로 받는다). 그래서 단일변수 규칙과 무관하다. t23 덱의 옛 이름
(2=DY1C, 3=DYTT)이 §5.24D 의 오독 사고를 냈으므로, P4 에서는
V2_7P 배치로 맞춰 원천 차단한다. 추출할 때 `--sdv-layout` 을 신경
쓸 필요가 없어진다.

## 발사 (병렬 가능 — 창 3개, 10코어씩 = 30/32)

`setup_p4.py --apply` 가 경로까지 채워 찍어준다. job 이름은
`CSIC_t23_p4` / `CSIC_t500_p4` / `CSIC_t1000_p4`.

## 발사 30분 뒤 볼 것

1. 냉각이 정상 진행하는가 — **냉각은 P3 와 같은 속도여야 한다**
   (A1T 는 냉각 손상에 거의 무관. 다르면 그 자체가 이상 신호)
2. 인장 스텝에서 컷백이 늘 수 있다 — A=50 은 연화가 급하다.
   증분이 1e-7 바닥에 붙어 안 움직이면 화면을 보낼 것

## 중간 엿보기 — 이틀 태우기 전에 A=50 이 맞는지 본다 (권장)

A=50 은 모델이 허용하는 **최대 연화**다. 과하면 최대점 근처에서
수렴이 깨질 수 있고(사전등록 셋째 갈래), 그때는 세 잡을 다 버려야
한다. 인장 스텝이 30 % 쯤 지났을 때 곡선만 훔쳐보면 몇 시간 만에
안다. `--nodamage` 가 그 용도다 (손상 추출을 건너뛰어 빠르다).

```bat
cd /d E:\LTH\Try_P4
abaqus python E:\LTH\extract_tension.py CSIC_t23_p4.odb --nodamage --tag _P4PEEK
```

보는 것: 최대점이 이미 지나갔는가, 지났다면 몇 MPa 인가.

| 엿본 결과 | 판단 |
|---|---|
| 최대점 지나고 **하강 지속** | 의도대로. 세 잡 그대로 두고 밤새 |
| 최대점 128 MPa 근처 | **최대 성과 신호.** 그대로 완주시킬 것 |
| 최대점이 100 아래로 급락 | A=50 이 과하다. 완주는 시키되 A 중간값(20) 재배치 준비 |
| 증분이 1e-7 바닥, 스텝시간 정체 | 수렴 실패. **세 잡 다 죽이고** A=20 으로 재배치 |

## 끝난 뒤 추출

P3 와 동일한 5종, 태그만 `_P4` / `_P4T500` / `_P4T1000`.
`--sdv-layout` 은 **auto 로 충분** — setup 이 이름배치를 정정하므로
세 덱 모두 `SDV layout: new` 가 찍히는 것이 정상이다.

## 판정표 (§5.25 사전등록 — 요약)

| P4 결과 | 결론 |
|---|---|
| 종국 연화 등장 + 최대점 하락 | 재경화 원인 = 완만한 A1T 확정 |
| 재경화 여전 | 연화지수 아님 → 기지 소성(§5.15 B1) 재부상 |
| 최대점 근처 수렴 실패 | eta 대비 과급 — A 중간값 재배치. 이것도 결과 |

부수 예측 5개는 §5.25 참조. 특히 **23 °C 가 128±10 MPa 에 오면
"논문 자신의 XT + 크랙밴드 상당 연화 = Table 3" 이 되어 이
프로젝트 최대 성과.**
