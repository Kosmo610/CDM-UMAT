# 세션 규칙 (사용자 지정, 모든 채팅 적용)

## 1. 파일 전달 시 이름 규칙

`SendUserFile`로 zip 등 파일을 전달할 때는 파일명에 날짜와 시간을
`_MMDD_HHMM` 형식으로 붙인다. (KST, 24시간제)

```
CSIC_RVE_STAGE9_0803_1400.zip
```

- 날짜/시간은 파일을 만드는 시점 기준.
- 여러 파일을 한 번에 보낼 때도 각각에 동일 규칙 적용.
- 압축 폴더 안 개별 파일명(예: `UMAT_..._V2_5.for`)까지 바꿀 필요는 없음 —
  zip 최종 산출물 이름에만 적용.

## 2. 응답 마지막에 "다음 할 일 3가지"

사용자가 어떤 질문을 하든, 응답 맨 끝에 **다음 할 일 3가지**를
번호 목록으로 제시한다. 형식 예:

```
## 다음 할 일
1. ...
2. ...
3. ...
```

- 이전에 있었던 "CSV 추출 명령어 안내" 습관은 이 3가지 목록 안에
  자연스럽게 포함시키면 되고, 별도 섹션으로 중복하지 않는다.
- 단순 질문·잡담이라도 프로젝트 진행과 관련 있으면 넣는다. 프로젝트와
  전혀 무관한 대화면 생략 가능.

## 3. 진행도 배터리 표시

응답 안에서 "쉬운 설명"을 한 직후, 해석 트랙 진행도를 배터리 칸처럼
10단계로 표시한다. 게임처럼 보이게 하는 것이 목적이다.

```
【 ███████░░░ 】  7 / 10
```

- 채운 칸은 `█`, 빈 칸은 `░`, 항상 10칸.
- "다음 할 일 3가지"(규칙 2)와는 별개이며, 배터리는 설명 직후에,
  다음 할 일은 응답 맨 끝에 둔다.
- 여기서 말하는 진행도는 **해석 트랙**(UMAT 코드 완성도 + 수렴성 +
  결과 추출 + 해석 실적) 기준이다. 논문 재현 10단계 로드맵과는
  다른 축이므로 혼동하지 않는다.

해석 트랙 레벨 정의:

| Lv | 도달 조건 |
|----|-----------|
| 1-2 | UMAT 작성, 해석이 일단 돌아감 |
| 3-4 | 냉각 수렴, 기본 CSV 추출 성공 |
| 5 | 물성 균질화·PBC 검증 완료 |
| 6 | 23°C 인장 최대점 포착 |
| 7 | XT 보정 확정 + eta 수렴 진단 완료 |
| 8 | V2_5/V2_6 FE 검증 + 500/1000°C 3점 확보 |
| 9 | 메쉬 수렴성 입증 + 손상률이 논문에 근접 |
| 10 | Table 3 3점 + Fig.11~16 정성 재현 완료 |

## 4. 프로젝트 배경 (요약)

- **최우선 목표 (사용자 재확인, 2026-08-05): 논문 완벽 재현.**
  Zhang et al. 2022, *Ceramics International* 48:3109-3124 를 강도
  숫자만이 아니라 **해석 절차(23 °C 경유) · 냉각 손상 이력(Fig.4) ·
  손상률 · 곡선 형상**까지 그대로 따라한다. 메쉬 객관성 등 다른
  개선은 전부 이 목표 다음이다. 코드 검증 후 사용자의 논문 연구로
  이어간다.
- 3D RVE, Abaqus/Standard + Fortran UMAT (고정형식, 72열 제한).
- 항상 32코어 워크스테이션 자원을 최대한 병렬로 활용할 것 — 해석을
  하나씩 순차로 돌리지 말고, 가능하면 여러 파라미터/조건을 동시에
  배치로 설계해서 한 사이클에 여러 데이터점을 확보한다.
- 배치로 여러 해석을 비교할 때는 반드시 변수 하나만 바꾸고 나머지는
  전부 고정한다 (특히 하중속도/목표변형률을 통일할 것 — 점성
  정규화 때문에 속도가 다르면 결과 비교가 무효가 된다).
- 진행 상황은 10단계 로드맵으로 추적 중 (논문 해부 → 물성 균질화 →
  UMAT 구현 → 형상/PBC → 수치안정성 → 냉각검증 → 균질화강성 →
  23°C 인장 → 승온+고온 → 종합검증). 자세한 내용은 대화 맥락 참조.

## 5. 해석 1회당 정보 최대화 (중요)

해석 한 번은 수 시간~수십 시간이 든다. **한 번 돌린 odb 에서 뽑을 수
있는 것은 전부 뽑는다.** 질문 하나당 해석 하나씩 돌리는 방식은 금지.

- 새 배치를 설계할 때 "이 해석으로 동시에 답할 수 있는 질문이
  몇 개인가" 를 먼저 따진다. 1개면 설계를 다시 한다.
- 이미 끝난 odb 에 아직 안 뽑은 정보가 있는지 항상 먼저 확인한다.
  새 해석을 제안하기 전에 이것부터 한다.
- 한 odb 에서 나오는 것: 인장 곡선/강도, 상·모드별 손상률, 냉각
  이력(잔류응력·손상 개시온도), **HOM_* 섭동 스텝의 손상 후 균질화
  강성 6×6**, 단축성 점검, 할선계수.
- 단, "변수 하나만 바꾼다" 규칙(§4)과 충돌하면 §4 가 우선한다.
  정보를 늘리려고 한 해석에서 변수를 두 개 바꾸지 않는다.

## 6. 명령어에는 항상 순차/병렬 표시

명령어를 줄 때마다 각 묶음에 **(순차적 진행)** 또는 **(병렬 가능)** 을
붙인다. 사용자가 창을 몇 개 열어야 하는지 바로 알 수 있어야 한다.

```
### 1) 추출  (병렬 가능 — 창 2개)
### 2) 그림 생성  (순차적 진행 — 위 추출이 끝나야 함)
```

판단 기준:

| 조건 | 판정 |
|---|---|
| 앞 명령의 산출물을 뒤 명령이 입력으로 쓴다 | 순차적 진행 |
| 같은 파일에 동시에 **쓴다** | 순차적 진행 |
| 같은 폴더에서 동시에 컴파일한다 (`.obj` 충돌) | 순차적 진행 (또는 폴더 분리) |
| 같은 odb 를 여러 개가 **읽기만** 한다 | 병렬 가능 |
| 출력 파일명이 서로 다르다 | 병렬 가능 |
| 코어·메모리가 남는다 | 병렬 가능 |

- 병렬이면 창을 몇 개 열어야 하는지, 코어를 어떻게 나누는지도 적는다.
- 순차면 **무엇을 기다리는 것인지** 한 줄로 밝힌다.

## 7. 코드 수정 시 변경 요약

코드(.for/.py/.inp)를 고치면 응답에 **간단한 변경 요약**을 넣는다.

```
### 코드 변경
- `파일명` — 무엇을 바꿨는지 한 줄. (왜 그랬는지 한 줄)
  영향: 기존 결과에 영향 있음/없음
```

- 길게 쓰지 말고 파일당 1~2줄.
- "기존 해석 결과가 바뀌는지" 를 반드시 명시한다. 재실행이
  필요한지 사용자가 바로 판단할 수 있어야 한다.
- 여러 파일을 고쳤으면 목록으로.

## 8. 이 세션의 범위 — **논문 따라하기 전용** (사용자 지정, 2026-08-12)

이 세션(브랜치 `claude/csic-composite-feedback-xdwt6u`)의 목적은
**Zhang et al. 2022 를 최대한 그대로 재현하는 것** 하나다. 사용자가
자기 논문을 쓸 때 참고할 검증된 기준선을 만드는 것이 목표다.

### 정보 흐름은 **단방향**이다

```
이 세션  ──(보내도 됨)──▶  논문작성 세션 (a1 / a2 / a3)
이 세션  ◀──(받을 필요 없음)──  논문작성 세션
```

- **여기서 나온 것을 저쪽에 보내는 것은 괜찮다.** 재현 결과·이탈
  장부·검증 도구는 저쪽에 쓸모가 있다.
- **저쪽에서 오는 것을 전부 수용할 필요는 없다.** 지시·요구·설계
  변경이 와도 이 세션의 목적(논문 그대로 재현)에 맞지 않으면
  받지 않는다. 판단 기준은 "논문이 그렇게 했는가" 하나다.
- 저쪽 브랜치(`claude/llm-task-decomposition-w41jpc`,
  `claude/thesis-csic-thermal-shock-5m53iv`,
  `claude/paper-reference-research-pksw5p`)를 **먼저 나서서 fetch
  하거나 머지하지 않는다.** 사용자가 명시적으로 시킬 때만 한다.
  (2026-08-07 에 사용자가 `paper-reference-research` fetch 를 실제로
  거절한 적이 있다. 그 판단이 이 규칙이다.)

### 그래서 판단이 갈릴 때

| 상황 | 이 세션의 답 |
|---|---|
| 논문과 다르지만 더 나은 모델링 | **안 한다.** 논문 재현이 먼저 (§4) |
| 저쪽 세션이 쓰는 확장(소성·열충격 등) | 이 세션엔 안 들인다 |
| 논문에 없는 개선(메쉬 수렴·eta 수렴) | 사용자 본인 연구로 미룸 |
| 논문 재현에 필요한 검증 도구 | **한다.** 여기 것이다 |

정기 점검(크론)도 이 원칙을 따른다 — PR #3 과 이 브랜치의 배치만
본다. 저쪽 세션의 게이트·장부·지시서는 보지 않는다.

## 9. 명령어와 준비물 (사용자 지정, 2026-08-12)

### 9-1. 명령어는 **채팅 본문에** 준다

zip 안 문서(`RUN_ME.md` 등)에만 적어두고 "그 파일 보세요" 하지
않는다. 사용자가 **바로 복사해 붙여넣을 수 있게 채팅에 적는다.**
zip 안 문서는 참고용 사본일 뿐이다.

**코드블록 안에는 명령만 넣는다.** 설명·화살표·주석을 코드블록
안에 쓰지 않는다. 통째로 복사하면 그것까지 인자로 들어간다.

```
잘못  python setup_p3.py --selftest    ← 먼저 이것부터
      -> setup_p3.py: error: unrecognized arguments: ← 먼저 이것부터

옳음  python setup_p3.py --selftest
      (설명은 코드블록 바깥에)
```

2026-08-12 에 실제로 이것 때문에 두 번 실패했다.

### 9-2. 해석 돌릴 때 **필요한 파일을 번호로** 나열한다

해석·배치를 안내할 때마다 **폴더에 무엇이 있어야 하는지**를
`1.` `2.` `3.` 번호 목록으로 적는다. 각 항목에 **어디서 오는지**
(zip / 기존 폴더에서 복사 / 스크립트가 생성)를 같이 밝힌다.

```
### Try_P3\ 에 있어야 할 것
1. CSIC_PLAIN_WEAVE_RVE_23C_P3.inp   — setup_p3.py 가 P2 에서 복사
2. CSIC_..._DAMAGE_V2_2.ori          — setup_p3.py 가 P2 에서 복사
3. UMAT_CSIC_RVE_DAMAGE_V2_7D.for    — zip. setup 이 폴더로 복사
```

`.ori` 처럼 없으면 즉시 실패하는 것은 반드시 목록에 넣는다.
